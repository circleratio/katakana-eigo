# 設計書 (spec.md)

本ドキュメントは `requirement.md` の要件を実現するための技術設計を定義する。

## 1. 実装方針

| 項目 | 決定 |
|---|---|
| 実装言語 | Python 3 |
| 発音辞書 | CMU Pronouncing Dictionary (`cmudict` / `pronouncing`) |
| 未収録語の発音推定 | grapheme-to-phoneme (`g2p_en`) によるフォールバック |
| 発音基準 | アメリカ英語(米語) |

CMU Pronouncing Dictionary は ARPAbet 表記で発音を表す約13万語収録の無料辞書。辞書に無い語(固有名詞・専門用語・造語など)は `g2p_en` の推定モデルで ARPAbet 系列を生成し、辞書引きと同じ変換パイプラインに乗せる。

## 2. 処理フロー

```
標準入力 or ファイル
        │
        ▼
   [1] read_input        テキストをUTF-8で読み込む
        │
        ▼
   [2] tokenize           英単語トークンとそれ以外(空白/句読点/改行/日本語等)に分割
        │
        ▼
   [3] resolve_phonemes   各英単語トークンをARPAbet音素列に変換
        │                 (CMU辞書 → 未収録ならg2pフォールバック)
        ▼
   [4] phonemes_to_kana   音素列をカタカナ表記に変換
        │
        ▼
   [5] reassemble         非英単語トークンをそのまま残しつつカタカナで置換して再結合
        │
        ▼
   [6] write_output       標準出力に書き出す
```

単語以外のトークン(空白・改行・句読点・日本語文字列など)は一切変更せずそのまま出力に残すため、元のレイアウトが保持される。

## 3. モジュール構成

```
katakana_eigo/
├── __init__.py
├── cli.py               # エントリポイント。引数解析、全体のパイプライン制御
├── io_utils.py           # read_input() / write_output()
├── tokenizer.py           # tokenize(): テキスト → Token のリスト
├── pronounce.py           # word_to_phonemes(): 単語 → ARPAbet音素列
├── kana.py                # phonemes_to_kana(): 音素列 → カタカナ文字列
└── data/
    └── phoneme_kana_map.py  # ARPAbet → かな の対応テーブル、変換ルール定数
tests/
├── test_phoneme_kana_map.py
├── test_tokenizer.py
├── test_pronounce.py
├── test_kana.py
├── test_io_utils.py
├── test_cli.py
├── test_integration.py     # fixtures/ を使った結合テスト
└── fixtures/               # 入力txt / 期待出力txt のペア(結合テスト用)
```

### 3.1 `cli.py`
- `argparse` でコマンドライン引数を解釈する。
- 使い方: `katakana-eigo [file]`
  - `file` (省略可): 入力ファイルパス。省略時は標準入力を読む。
  - `-h` / `--help`: 使用方法を表示(`argparse` 標準機能)。
- `read_input` → `tokenize` → 各単語トークンに `pronounce` → `kana` を適用 → `reassemble` → `write_output` の順にパイプラインを呼び出す。
- 例外処理はこのモジュールで一元的に捕捉し、`sys.exit(code)` で終了する(詳細は 6章)。

### 3.2 `io_utils.py`
- `read_input(path: str | None) -> str`
  - `path` が `None` の場合: `sys.stdin` から全文読み込み(UTF-8)。
  - `path` が指定された場合: 該当ファイルをUTF-8で開いて全文読み込み。存在しない/読み込み権限がない場合は例外を送出し、`cli.py` 側で捕捉する。
- `write_output(text: str) -> None`
  - `sys.stdout` に書き出す。
- **UTF-8強制について**: `sys.stdin` / `sys.stdout` はOS・ロケール依存のエンコーディング(Windows環境では既定で `cp932` になることを実装時に確認済み)になっている場合があるため、実処理の前に `reconfigure(encoding="utf-8")` で明示的にUTF-8へ切り替える(`_force_utf8` ヘルパー)。テストで `io.StringIO` 等に差し替える場合は `reconfigure` を持たないため `hasattr` で存在確認してから呼び出す。

### 3.3 `tokenizer.py`
- `tokenize(text: str) -> list[Token]`
- `Token` はデータクラス: `text: str`, `is_word: bool`
- 英単語の判定は正規表現 `[A-Za-z]+(?:'[A-Za-z]+)*` を用いる(`don't` `it's` のような内部アポストロフィを含む語を1トークンとして扱う。アポストロフィが連続する場合も対応)。
- それ以外の連続文字(空白・改行・句読点・日本語・数字・記号など)は非単語トークンとしてまとめ、変更せず保持する。

### 3.4 `pronounce.py`
- `word_to_phonemes(word: str) -> list[str]`
  1. `word` を小文字化して CMU 辞書 (`pronouncing.phones_for_word`) を引く。
  2. 複数の発音候補がある場合は先頭(最も一般的とされる表記)を採用する。
  3. 辞書に存在しない場合、`g2p_en` により音素列を推定する。
  4. それでも取得できない場合(推定処理自体が失敗した場合)は空リストを返す。呼び出し側 (`kana.py` もしくは `cli.py`) はこれを「変換不能」とみなし、元の単語をそのまま出力する。
- 戻り値はストレス数字付きの ARPAbet 音素列(例: `like` → `['L', 'AY1', 'K']`)。

### 3.5 `kana.py`
- `phonemes_to_kana(phonemes: list[str]) -> str`
- ARPAbet音素列の先頭から走査し、子音の直後に母音が続く場合はその場で合成、続かない場合(語末・子音クラスタ)は母音を補って処理する、という単純な先読み1つの状態機械で実装した(音節を事前にグルーピングする2段階処理ではない)。
- 実装した変換ルール:
  - **子音+母音の合成**: `CONSONANT_MAP[子音][母音.column] + 母音.kana[1:]` で合成する。母音の先頭仮名を子音側の行に置き換え、長音符・二重母音の後半はそのまま付け足す(例: `T` + `IY`(イー) → `ティ` + `ー` = `ティー`)。
  - **ストレスは母音の種類選択に使わない**: 無強勢のシュワー(`AH0`)も強勢のある `AH` と同じ仮名表現を使う(簡略化。`banana` → `バナナ`、`about` → `アバウト` 等で実用上妥当な結果になることを確認済み)。
  - **子音クラスタ・語末の母音補完**: 後続に母音が無い子音は既定で "u" 列(ウ段)を補う。`T` / `D` のみ "o" 列(ト/ド)を使う(外来語表記の慣習に合わせた例外)。例: `desk` → `デスク`。
  - **N の特別扱い**: 後続に母音が無い `N` は「ン」(撥音)にする。「ヌ」にはしない(例: `pen` → `ペン`)。
  - **促音「ッ」**: 語末の無声破裂音・破擦音(`P T K CH`)の直前が「短母音」(`VOWEL_MAP` 上でかな表記が1文字の母音: `AA AE AH AO EH IH UH`)の場合にのみ促音を前置する(例: `cat` → `カット`, `book` → `ブック`)。二重母音・長母音(`AY EY OW ER IY UW` 等)の後には付与しない(例: `like` → `ライク`)。
  - **Rの長音化(コーダR)**: 母音の直後にあり、かつ自身の後ろに母音が続かない `R` は、独立した子音(ラ行)ではなく直前母音の長音「ー」として扱う。アメリカ英語のRカラー母音を簡易的に近似するための規則(例: `car` → `カー`, `party` → `パーティー`)。
  - **L と R は区別しない**: どちらも `CONSONANT_MAP` 上は同じラ行として変換する。標準的なカタカナの表現力の限界であり、`requirement.md` に既知の限界として明記している。
  - 上記ルールは `kana.py` 内の定数(`_EPENTHETIC_COLUMN_OVERRIDES`, `_GEMINATION_CONSONANTS`, `_SHORT_VOWELS` 等)として実装し、`data/phoneme_kana_map.py` のテーブルを参照する。
- 本ルールは規則ベースのベストエフォートであり、定着した外来語表記と一致しない場合がある(例: `stop` は `ストップ` ではなく `スタップ` になる。`AA` を一貫して "ア" 列として扱う簡略化に起因)。`requirement.md` の「既知の限界」を参照。

### 3.6 `data/phoneme_kana_map.py`
- ARPAbet音素(ストレス数字なし)をキーとした基本かな対応表 `VOWEL_MAP`(母音15種)・`CONSONANT_MAP`(子音24種)と、ストレス数字を分離する `normalize_phoneme()` を定義する。
- `VOWEL_MAP` の各エントリは `column`("a"/"i"/"u"/"e"/"o")と単独時のかな表記 `kana` を持つ(`VowelKana` データクラス)。
- `CONSONANT_MAP` の各エントリは a/i/u/e/o の5列ぶんのかな表記を持つ辞書。
- 代表例(全量ではない):

| ARPAbet | 例の仮名 | 備考 |
|---|---|---|
| AA | ア | |
| AE | ア | |
| AH | ア | 強勢の有無を問わず同じ仮名 |
| IY | イー | 長音化 |
| EY | エイ | |
| OW | オウ | |
| UW | ウー | |
| ER | アー | R色付き母音 |
| L | ラ行 | Rと同一表記 |
| R | ラ行 | 母音直後・後続母音なしの場合は長音「ー」に変わる(kana.py側の規則) |
| TH | ス行 | 無声th |
| DH | ズ行 | 有声th |
| V | ヴ行 | |
| SH | シュ | |

## 4. 単語以外の扱い

- 英単語トークン以外(日本語・数字・記号・空白・改行)は変換対象外とし、`tokenize` が切り出した文字列をそのまま出力に残す。
- これにより、日英混在テキストでも英単語部分だけがカタカナに置換される(要件通り)。

## 5. CLI仕様

```
使用法:
  katakana-eigo [FILE]

引数:
  FILE   変換対象の英語テキストファイル(省略時は標準入力から読み込む)

オプション:
  -h, --help   使用方法を表示して終了する
```

- 出力は常に標準出力へ書き出す(v1ではファイル出力オプションは設けない。将来拡張候補は 8章参照)。

## 6. エラーハンドリング・終了コード

| ケース | 挙動 | 終了コード |
|---|---|---|
| 正常終了 | 変換結果を標準出力へ出力 | 0 |
| 指定ファイルが存在しない | `エラー: ファイルが見つかりません: <path>` を標準エラー出力へ出力 | 1 |
| 指定ファイルの読み込み権限がない/読み込みエラー | `エラー: ファイルを読み込めません: <path>` を標準エラー出力へ出力 | 1 |
| 個々の単語の発音推定・変換に失敗 | その単語は原文のまま出力し、処理は継続する(全体は中断しない) | - |
| 上記以外の予期しない例外 | スタックトレースを標準エラー出力へ出力 | 2 |

「未収録語の変換失敗」は要件上の正常系(ベストエフォート)として扱い、プロセス全体のエラーとはしない。

## 7. 依存ライブラリ

- `pronouncing`(CMU Pronouncing Dictionaryのラッパー)
- `g2p_en`(未収録語の発音推定、内部で `nltk` のデータを使用)
- 標準ライブラリ: `argparse`, `re`, `sys`, `dataclasses`

## 8. 今後の課題

- 出力をファイルへ書き出すオプション(`-o`)→ v1では設けない方針に確定(`requirement.md` 参照)。将来必要になれば追加を検討。
- 巨大な入力ファイルに対するストリーミング処理(現状は全文をメモリに読み込む前提。未着手)
- `g2p_en` の依存(NLTKデータのダウンロードや実行時サイズ)が許容できない場合の軽量な代替推定ルールの検討
  - 実装時点では `g2p_en` は `tensorflow` 等を必要とせず `numpy`/`nltk` ベースで動作することを確認済みで、当面は許容範囲と判断。
- 複合語・略語(例: `NASA`, `USA`)をアルファベット読みするか単語として読むかの個別ルール(未実装。現状は通常の単語として発音推定される)
- L/R の区別、`stop`→`スタップ` のような定着した外来語表記との不一致など、規則ベース変換に起因する精度上の限界(`requirement.md` の「既知の限界」参照)

## 9. テスト方針

- `phoneme_kana_map.py` / `tokenizer.py` / `pronounce.py` / `kana.py` / `io_utils.py` / `cli.py` はそれぞれ単体テストで入出力を検証する(`tests/test_*.py`)。
- `tests/fixtures/` に入力テキストと期待されるカタカナ出力のペアを用意し、`tests/test_integration.py` でCLI全体を通した結合テストを行う。期待値は実行結果をゴールデンファイル化したもので、内容は目視レビュー済み。
- 日英混在テキスト、未収録語(g2pフォールバック)、句読点・改行・アポストロフィを含むテキストなど、要件で挙げたケースを網羅する。
- 2026-08時点で全76件のテストが通過。
